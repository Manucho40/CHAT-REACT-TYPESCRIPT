import React, { FC } from 'react'
import { Button } from 'antd';
const Header: FC = () => {
  return (
    <div>
        <header>
            <Button type="primary" block>
                CONNEXION
            </Button>
        </header>
    </div>
  )
}

export default Header;
